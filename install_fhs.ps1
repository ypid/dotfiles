## Create https://en.wikipedia.org/wiki/Filesystem_Hierarchy_Standard on Windows.
## Some application configs here might use/require those directories.

mkdir -f "C:/var/log/kitty_sessions"
mkdir -f "C:/var/tmp"
mkdir -f "C:/tmp"
