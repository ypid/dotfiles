## Cleaner git ignore file (compared to .gitignore_global_old).
## Only ignore files which are unique to my setup.
## git repositories should be self sustained.


## Editor specific files
# *.*~
*.bak

## Vim documentation index for each plugin. Should be ignored by the plugin repository but some don’t do this …
doc/tags

## TeX
*.tex.backup
*.sty.backup

## Python rope (https://github.com/python-rope/rope)
.ropeproject
